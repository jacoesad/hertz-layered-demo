server:
  addr: ":8889"

database:
  driver: sqlite3
  dsn: data/hz-server.db

starrocks:
  driver: sqlite3
  dsn: data/starrocks.db

downstream:
  task_runner:
    endpoint: http://127.0.0.1:9000
    timeout_ms: 3000
